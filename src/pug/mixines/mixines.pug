//mixin link(className, href,name)
//   a(class= className)(href= href)= name

mixin menuMix(listName, linkName)
   each i in menu
      li(class= listName)
         +link(linkName, i.url, i.name)




